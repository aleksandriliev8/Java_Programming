package problem5;

public class integerEquivalents {
    public static void main(String[] args) {
        byte byteEx;
        short shortEx;
        int intEx;
        long longEx;

        byteEx ='A';
        shortEx ='A';
        intEx ='A';
        longEx ='A';

        System.out.format("The integer equivalents of A are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);


        byteEx ='B';
        shortEx ='B';
        intEx ='B';
        longEx ='B';

        System.out.format("The integer equivalents of B are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx ='C';
        shortEx ='C';
        intEx ='C';
        longEx ='C';

        System.out.format("The integer equivalents of C are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx ='a';
        shortEx ='a';
        intEx ='a';
        longEx ='a';

        System.out.format("The integer equivalents of a are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx ='b';
        shortEx ='b';
        intEx ='b';
        longEx ='b';

        System.out.format("The integer equivalents of b are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx ='c';
        shortEx ='c';
        intEx ='c';
        longEx ='c';

        System.out.format("The integer equivalents of C are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx = '0';
        shortEx = '0';
        intEx = '0';
        longEx = '0';

        System.out.format("The integer equivalents of 0 are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx = '1';
        shortEx = '1';
        intEx = '1';
        longEx = '1';

        System.out.format("The integer equivalents of 1 are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx = '2';
        shortEx = '2';
        intEx = '2';
        longEx = '2';

        System.out.format("The integer equivalents of 2 are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx = '$';
        shortEx = '$';
        intEx = '$';
        longEx = '$';

        System.out.format("The integer equivalents of $ are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx = '*';
        shortEx = '*';
        intEx = '*';
        longEx = '*';

        System.out.format("The integer equivalents of * are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx = '+';
        shortEx = '+';
        intEx = '+';
        longEx = '+';

        System.out.format("The integer equivalents of + are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx = '/';
        shortEx = '/';
        intEx = '/';
        longEx = '/';

        System.out.format("The integer equivalents of slash are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);

        byteEx = ' ';
        shortEx = ' ';
        intEx = ' ';
        longEx = ' ';

        System.out.format("The integer equivalents of whitespace are: %d,%d,%d,%d%n", byteEx, shortEx, intEx, longEx);
    }
}
